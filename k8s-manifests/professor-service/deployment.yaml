apiVersion: apps/v1
kind: Deployment
metadata:
  name: professor-tecadm-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: professor-tecadm
  template:
    metadata:
      labels:
        app: professor-tecadm
    spec:
      containers:
        - name: professor-tecadm-service
          image: distrischool-professor-tecadm-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8082 # Mantém a porta específica se necessário
          env:
            # --- Configurações de Banco de Dados ---
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://postgres-service:5432/distrischool_db" # <<< Banco unificado
            - name: SPRING_DATASOURCE_USERNAME
              value: "postgres" # <<< Credencial do .env
            - name: SPRING_DATASOURCE_PASSWORD
              value: "postgres" # <<< Credencial do .env
            # --- Configurações de Schema ---
            - name: SPRING_FLYWAY_SCHEMAS
              value: "professor_schema" # <<< Schema específico
            - name: SPRING_FLYWAY_CREATE_SCHEMAS
              value: "true"
            - name: SPRING_JPA_PROPERTIES_HIBERNATE_DEFAULT_SCHEMA
              value: "professor_schema" # <<< Schema específico

            # --- Configurações do RabbitMQ ---
            - name: SPRING_RABBITMQ_HOST
              value: "rabbitmq-service"
            - name: SPRING_RABBITMQ_PORT
              value: "5672"
            - name: SPRING_RABBITMQ_USERNAME
              value: "admin" # <<< Credencial do .env
            - name: SPRING_RABBITMQ_PASSWORD
              value: "admin" # <<< Credencial do .env